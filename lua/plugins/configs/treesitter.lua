local options = {
  ensure_installed = { "lua", "typescript", "go", "rust", "javascript"},

  highlight = {
    enable = true,
    use_languagetree = true,
  },

  indent = { enable = true },
}

return options
